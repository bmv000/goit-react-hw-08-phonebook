// import { useSelector } from 'react-redux';
// import { selectUser, selectIsLogin } from 'redux/auth/auth.selectors';

// export const useAuth = () => {
//   const isLoggedIn = useSelector(selectIsLogin);
//   const user = useSelector(selectUser);
//   const user = useSelector(selectUser);

//   return {
//     isLoggedIn,
//       user,
//   };
// };
